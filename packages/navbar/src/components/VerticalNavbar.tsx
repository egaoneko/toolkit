import { FC } from 'react';
import { Button } from '@toolkit/shared';

import 'src/styles';

const VerticalNavbar: FC = () => {
  return (
    <div>
      <span>VerticalNavbar</span>
      <Button>Button</Button>
    </div>
  );
};

export default VerticalNavbar;
